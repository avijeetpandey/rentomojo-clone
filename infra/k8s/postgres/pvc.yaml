apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-pvc
  namespace: rentomojo
  labels:
    app.kubernetes.io/name: postgres
    app.kubernetes.io/part-of: rentomojo
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
