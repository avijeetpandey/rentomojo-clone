apiVersion: v1
kind: Service
metadata:
  name: backend-svc
  namespace: rentomojo
  labels:
    app.kubernetes.io/name: backend
    app.kubernetes.io/part-of: rentomojo
spec:
  selector:
    app: rentomojo-backend
  ports:
    - name: http
      port: 4000
      targetPort: 4000
      protocol: TCP
  type: ClusterIP
